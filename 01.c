//w.a.p to check whether a given no. is +ve or non +ve.
main()
{
    int a;
    printf("enter a number:");
    scanf("%d",&a);
    if(a>0)
    printf("the given number is positive.");
    else
    printf("The given number is non-positive.");
}